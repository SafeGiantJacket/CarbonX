type Listing = record { seller : principal; price : nat; amount : nat };
type Marketplace = service {
  buy : (nat, nat) -> ();
  getListings : () -> (vec Listing) query;
  list : (nat, nat) -> (nat);
};
service : Marketplace